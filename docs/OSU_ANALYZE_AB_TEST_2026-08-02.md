# OSU Analyze Validator 大减法 A/B 测试报告（2026-08-02）

依据用户指令执行：停止单词/短语/否定前缀/修辞补丁，将 validator 重构为小型高精度事实保险层；C 类语义判断交给 LLM reviewer（只记录意见，不重写）；删除 D/E 类。

## 1. 变更摘要

- 删除：`causePattern` 动机/状态词表、偏好、PP+ 能力化系列、稳定断言、堆量/经历、萌新词表、档位/群体比较（除 B 类通用规则）、追星动作、社区用语黑名单、所有单句补丁（引号豁免、否定前缀、顿号隔离等）。
- 保留（A 类）：数字白名单、PP+ 维度-数值绑定、Mod 精确/包含数量、分类数量、星数阈值、BP1 满准、统计夸写/忽略、结构/长度/JSON、身份代词、术语（Top/HD/DDT/PA/准心/alt→跳图/在线时长/年龄换算/游玩次数约数/倍数）、简报明确未提供内容（设备/身体/replay/谱面细节）。
- 保留（B 类，合并为 4 条通用规则）：空 Recent 的具体状态断言、聚合均值→同难度单图、无来源明确群体比较（超过绝大多数/全球前 X%/全世界只有）、声称不存在简报中存在的数量（98% 以上/低于 95% 对称）。
- Reviewer：只记录 per-section 意见进 reviewLog，不再触发重写或降级。
- 生成轨迹：每次 attempt 的 raw draft 落 `generationTrace[].draft`。

## 2. 三锚点 ×3 结果（v85-raw-ab-1/2/3）

| 批次 | mrekk | Wuxin | ahahhaha |
|---|---|---|---|
| ab-1 | LLM（首稿直通） | LLM（首稿直通） | LLM（首稿直通） |
| ab-2 | LLM（首稿直通） | LLM（首稿直通） | LLM（首稿直通） |
| ab-3 | LLM（首稿直通） | LLM（1 次重写） | LLM（首稿直通） |

结论来源：9/9 LLM，fallback 0。结论首稿直通 8/9（88.9%）。

## 3. raw draft vs final

- 8/9 的 final 即 raw draft（无后处理）。
- Wuxin ab-3：attempt1 因星数 claims 串线误杀（"每1张成绩上、用 7★+"被读成"1 张 7★+"）→ 修复（间隔 16→4 字符）；attempt2 因真实身份错误（"他"）被拒 → attempt3 通过。最终稿数据全对，与首稿同质。
- 后处理（重写）仅 1/9，且由真实硬错误（代词）触发；未发现"最终稿劣于 raw draft"的退化案例。

## 4. 新 validator 拒绝原因分布（全部 A/B 硬门）

9 份中所有机械拒绝均为可证明错误：

- 简报外数字：1.65、58、10、6000/600000、0.4
- 字段绑定：Accuracy 15.67 ≠ 14.58
- Mod 精确数量：HR 3 张不是 2 张、HDNC 1 张不是 2 张、HDDT 3 张不是 4 张
- 星数阈值：70 张 ≥7★ 不是 1 张（串线，已修复）
- 身份：中文数词换算年龄
- 空 Recent 具体状态断言

语义/文风误杀：0 条。

## 5. 旧 validator 会拒绝、新 validator 放行的文本（抽样）

以下表达现均通过机械 validator（语义交由 reviewer 记录）：

- "Speed 是六维最低项，所以速度不是这人的菜"
- "擅长单点跳跃、点得准的玩家"
- "NM 是主食，少量 HR 像是偶尔开着玩"
- "这些 NF 像是开着不死去够更高难度的图"
- "这个体量下还能长期保持 98.80% Acc"
- "等级 101（48%）说明账号还在持续往前推"
- "A 有 1200 张、S 只有 340 张，和总 Acc 98.80% 形成反差"
- "BP5 看起来不像同一次状态打出来的"
- "看来还没有完全吃透"
- "这批 BP 成绩已经相当扎实，整个列表都收稳了"
- "总 Acc 摆在这里，说明这个人已经很有准头"
- "闷头打图、正玩得开心"
- "先把准度钉住，接下来肯磨准度就会涨 pp"
- "多打几场让数据开口说话吧"
- "不是靠堆次数能摸到的，15420 次游玩慢慢积累出了家底"
- "难度下限本身就是一种态度，6.60★ 已经成了日常"
- "纯 NM 是绝对主场，不带 Mod 硬打才看得出真实底子"
- "靠稳定瞄准撑起来，加速也按得住"
- "这个分布真迷人，我都想凑过去看看屏幕了"
- "aim 是跳跃主菜，stream 是耐力配菜"
- "三种玩法都站得住"
- "速度和耐力把塔尖撑高"
- "BP1 那张 99.27% Acc 很干净，准头已经有意思了"
- "等记录再多一点，再看会往 alt 深处还是难度高处走"
- "敢开 HR 但还没收稳""这份专注""反复打磨出的厚度""离打磨成器还早"

## 6. 统计

| 指标 | 数值 |
|---|---|
| 旧 validator 误杀导致的 fallback（v84 时代近似） | Wuxin 3/9、ahahhaha 6/9 批次掉 fallback，绝大多数由语义词表误杀 |
| 新 validator 下 fallback 率 | 0/11（9 + 2 补充） |
| 结论首稿直通率 | 8/9（88.9%） |
| 硬事实错误漏判 | 1 处（ab-1 ahahhaha "连1张 98% 以上都没有"，实际 2 张）→ 已加对称 B 类规则并补跑 ab-4/ab-5 验证通过 |
| 后处理退化案例 | 0 |
| 语义/文风误杀 | 0 |

## 7. 结论

精简 validator（只保留可证明硬门）+ reviewer 只记录后，三锚点 9 份输出全部 LLM、零 fallback、首稿直通率 89%；机械拒绝全部为真实数字/Mod/身份错误；此前靠词表"假装拦住"的语义问题全部移交 reviewer 记录与人工审计。A/B 测试完成，可以进入九人黄金回归。

产物：`artifacts/osu-analyze-evals/2026-08-01T20-48-48-077Z-v85-raw-ab-*`
